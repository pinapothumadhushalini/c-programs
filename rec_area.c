#include<stdio.h>
int main()
{
	float length,breadth,area;
	printf("Enter length and breadth values");
	scanf("%f",&length,&breadth);
	area=length*breadth;
	printf("area=%f",area);
	return 0;
}
